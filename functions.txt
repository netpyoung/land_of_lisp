
2장
setf
defun - 전역 함수
defvar - 전역변수지정
defparameter - 전역변수지정
let - 지역 변수
flet - 지역 함수

3장
cond - cons cell생성
list - cons cell들을 역어 list로 만듬

4장
oddp - 홀인 경우 true
when - 참일때 수행
unless - 거짓일때 수행
cond - 조건식을 다수 가질수 있음.

and, or
eq - 심볼비교
equal - eq말고 다른것 모두
member - 값을 list에서 찾는다(찾은 것 부터 리스트 반환)
find-if - 함수를 인자로 받아 list를 찾는다.(찾은 값만 반환)

5장
assoc - list에서 키를 가지고 찾는 것
따옴표, 역따옴표, 쉼표
mapcar

6장
progn 각 list들을 수행
print 출력(라인및 공백추가)
prin1 출력
read 입력(쌍따옴표붙여야 string)
read-line 입력(쌍따옴표 안붙여도 string)
